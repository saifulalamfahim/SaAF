import React from 'react';

const WebDevDetails = (props) => {
    // console.log(props.detail);
    return (
        <div>
            <h1>This is Web Development Projects Details</h1>
        </div>
    );
};

export default WebDevDetails;